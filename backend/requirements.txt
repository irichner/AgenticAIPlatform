# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.3
python-multipart>=0.0.9

# Database
sqlalchemy==2.0.35
alembic==1.13.2
asyncpg==0.29.0
greenlet==3.1.1
pgvector>=0.3.0

# Cache / pub-sub
redis==5.1.0

# Validation
pydantic==2.9.2
python-dotenv==1.0.1

# LangGraph + LangChain
langgraph>=0.2.57
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# LangGraph checkpointing (HIL)
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.2.0

# MCP
mcp[cli]>=1.6.0
langchain-mcp-adapters>=0.1.0

# HTTP client (used by MCP adapters)
httpx>=0.27.0

# Anthropic SDK (used by langchain-anthropic)
anthropic>=0.40.0

# OpenAI SDK (used for embeddings)
openai>=1.30.0

# Document parsing
pypdf>=4.0.0

# Google OAuth (Google Drive integration)
google-auth-oauthlib>=1.2.0
google-auth>=2.29.0
